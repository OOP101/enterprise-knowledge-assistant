# =====================================================================
# 企业知识助手 依赖清单
# 安装：.venv\Scripts\python.exe -m pip install -r requirements.txt
# =====================================================================

# ---- 核心框架 ----
langchain>=0.3.0
langchain-core>=0.3.0
langchain-community>=0.3.0
langchain-openai>=0.2.0
langchain-text-splitters>=0.3.0
langgraph>=0.2.0

# ---- Web 服务 ----
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9            # 文件上传
python-dotenv>=1.0

# ---- 向量数据库（开发 ChromaDB，生产切 Milvus）----
# ⚠ 锁定 chromadb==1.3.7：1.5.x 在部分 Windows 环境下后台压缩器不落盘段数据文件，
#   会导致重启后 "Error loading hnsw index"（数据仍在 sqlite 队列，可回填修复，
#   服务启动自愈见 embedder.repair_chroma_segments）。升级前务必先在目标机器验证落盘。
chromadb==1.3.7
langchain-chroma>=1.1.0,<2.0
# langchain-milvus>=0.1.0          # 生产环境启用时取消注释

# ---- Embedding 与模型 ----
pydantic-settings>=2.0
dashscope>=1.14.0                  # DashScope SDK（text-embedding-v3，1024 维）
# sentence-transformers>=2.5.0     # EMBEDDING_PROVIDER=local 时启用（BGE，512 维）
# torch>=2.0                       # CPU 版，随 sentence-transformers 自动安装

# ---- 文档解析 ----
pymupdf>=1.24                      # PDF 文本提取（含 fitz）
python-docx>=1.1                   # Word 文档
# paddlepaddle>=2.6                # OCR 图片页（可选，体积大）
# paddleocr>=2.7                   # OCR 图片页（可选）

# ---- 中文分词（DemoEmbeddings / BM25 依赖）----
jieba>=0.42

# ---- 其他 ----
numpy>=1.26
